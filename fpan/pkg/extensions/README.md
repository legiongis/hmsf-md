# Package extensions are not stored here

For the time being, all extensions should be kept directly in the project directory, not within this package. This is to avoid duplicate files in version control.